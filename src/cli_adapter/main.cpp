#include <iostream>
#include "fcsim.h"

int main() {
    // read parameters from stdin
    int64_t max_ticks;
    std::cin >> max_ticks;
    // read design data from stdin
    ft_design_spec design;
    return 0;
}